#+begin_quote
[!WARNING]
*重要*: このプロジェクトの大部分はAIによって記述されています。全てのコードを確認しきれていないため、使用にあたっては注意してください。
#+end_quote

[[file:README.org][English]]

* Hoogle for CSharp

C# / .NET 向けの型シグネチャ検索ツールです。Haskell の [[https://hoogle.haskell.org/][Hoogle]] に着想を得ています。

** features
- 指定した型に一致する関数の一覧を表示します。
- BCL・プロジェクトの依存NuGetパッケージ・プロジェクト自体のソースコードを対象に検索可能。
- CLI向けの出力のほか、JSONでの出力にも対応。

** 必要環境

- .NET 8.0 SDK

** ビルド

#+begin_src sh
dotnet build
#+end_src

** 使い方

#+begin_example
Usage: csharp-hoogle [options] <query>

  <query>                       If it contains "->", matched as a type signature
                                (e.g. `IEnumerable<T> -> Func<T,bool> -> IEnumerable<T>`).
                                Otherwise, case-insensitive substring on the full name.
  --rebuild                     Rebuild the on-disk cache before searching.
  --list-assemblies             List assemblies in the cache with method counts.
  --json                        Emit results as JSON on stdout (for piping into tools).
  --polymorphic                 Include matches where a concrete query type binds a method's
                                declared generic parameter (e.g. Func.Invoke for Int32 -> Int32 -> Int32).
  --project <path>              Pin context to a specific .sln/.slnx/.csproj file.
  --target-framework <tfm>, -f  Pin TFM (e.g. net8.0); overrides on-disk detection.
  --max-results <int>           Set maximum number of results (default: no limit)
  --help                        Show this message.
#+end_example

オプションを指定しない場合、 =csharp-hoogle= はカレントディレクトリから親方向に
=.sln= / =.slnx= を探索する。見つからなければカレントディレクトリの =.csproj=
にフォールバックし、それも無ければ実行中の .NET ランタイムを対象とする。

*** 対象とするプロジェクトの決定

同一階層に ~.csproj~, ~.sln~, ~.slnx~ が存在する場合、 ~.sln~, ~/.slnx~ が優先される。

1. ~--project~ によってプロジェクトが決定されたプロジェクト
   1. ~--project~ がファイルを指している場合はそのファイル
   2. ディレクトリを指している場合はそのディレクトリ内にあるプロジェクトファイル
2. 現在のディレクトリにあるプロジェクトファイル
3. 上層回にあるプロジェクトファイル(再帰的に登る)

** 例

シグネチャ検索:

#+begin_src sh
csharp-hoogle "IEnumerable<T> -> Func<T,bool> -> IEnumerable<T>"
#+end_src

メソッド名の部分一致検索:

#+begin_src sh
csharp-hoogle Select
#+end_src

ポリモーフィックなシグネチャ検索（具体型がメソッドのジェネリック型引数を束縛
する一致も含める）:

#+begin_src sh
csharp-hoogle --polymorphic "int -> int -> int"
#+end_src

** テスト

#+begin_src sh
dotnet test
#+end_src
